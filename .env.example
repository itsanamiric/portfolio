# Optional. The site is static in v1 and runs without a database.

# Canonical URL for metadata, sitemap, and Open Graph.
# On Vercel this is inferred from VERCEL_PROJECT_PRODUCTION_URL if unset.
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Future: Neon + Drizzle. Not required to run or deploy this version.
# DATABASE_URL=
